# Domain modules

This folder is the center of product development.

Each `modules/<domain>` folder is a vertical slice containing specs, use cases, UX, contracts, domain code, core logic, DB ownership, web UI building blocks, tests, fixtures, and delivery evidence.

`apps/web` should only wire routes and runtime concerns to module exports.
