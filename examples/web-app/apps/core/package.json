{
  "name": "@ravenopsnet/core",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./auth": "./src/auth.ts"
  },
  "scripts": {
    "auth:generate": "pnpm dlx auth@latest generate --config src/auth.ts --yes",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ravenopsnet/config": "workspace:*",
    "@ravenopsnet/db": "workspace:*",
    "@ravenopsnet/domain": "workspace:*",
    "better-auth": "catalog:",
    "effect": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "vitest": "catalog:"
  }
}