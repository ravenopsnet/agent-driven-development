name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ci:
    name: Project CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: TODO install runtime/package manager
        run: echo "Configure runtime and package manager for this project"

      - name: TODO install dependencies
        run: echo "Configure dependency installation"

      - name: TODO run checks
        run: echo "Configure format, typecheck, lint, tests, and build"
